const names = ['a','b','c'];

const tickets = [];
for (let index = 0; index < names.length; index++) {
    const name = names[index];
    for (let i = 0; i < index + 1 ; i++) {
        tickets.push = name;
    }
}